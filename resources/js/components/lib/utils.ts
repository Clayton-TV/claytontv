// Shim: generated shadcn-vue components import "@/lib/utils", but this
// project's "@" alias points at resources/js/components (not resources/js).
// Re-export the real helper so the generated imports resolve.
export { cn } from '~/lib/utils';
